Now output intermediate result
/* Generated by Frama-C */
struct allocator {
   
};
struct vector {
   struct allocator alloc ;
};
void allocator::Ctor(struct allocator *this);

void allocator::Ctor(struct allocator *this, struct allocator const *x);

/*@ requires \valid(this); */
void allocator::Ctor(struct allocator *this)
{
  return;
}

/*@ requires \separated(this, x);
    requires \valid(this);
    requires \valid(x); */
void allocator::Ctor(struct allocator *this, struct allocator const *x)
{
  return;
}

void vector::Ctor(struct vector *this);

void vector::Ctor(struct vector *this, struct vector const *x);

void foo(struct vector *this);

/*@ requires \valid(this); */
void vector::Ctor(struct vector *this)
{
  allocator::Ctor(& this->alloc);
  return;
}

/*@ requires \separated(this, x);
    requires \valid(this);
    requires \valid(x); */
void vector::Ctor(struct vector *this, struct vector const *x)
{
  allocator::Ctor(& this->alloc,& x->alloc);
  return;
}

/*@ requires \valid(this); */
void foo(struct vector *this)
{
  struct vector tmp;
  vector::Ctor(& tmp);
  return;
}


