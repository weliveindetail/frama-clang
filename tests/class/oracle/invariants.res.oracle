[kernel] warning: ignoring status of global invariant `_ZN1A5x_posE'
[kernel] warning: ignoring status of type invariant `_ZN1A7pos_INTE'
[kernel] warning: ignoring status of type invariant `_ZN1A5y_valE'
/* Generated by Frama-C */
struct _Z1A;
typedef struct A _Z1A;
typedef int _ZN1A3INTE;
struct A {
   int _ZN1A1yE ;
};
static int x;
__inline void A::Ctor(struct A *this);
__inline void A::Ctor(struct A *this, struct A *__other);
__inline struct A *operator=(struct A *this, struct A *__other);
__inline void A::Dtor(struct A *this);
/*@ global invariant x_pos: x ≥ 0;
 */
/*@ type invariant pos_INT(INT z) = z ≥ 0;
 */
/*@ type invariant y_val(A *this) = this->y ≥ x;
 */
/*@ predicate __inv{L}(A *this) = this->y ≥ x;
 */
static int x = 0;
/*@ requires \valid(this);
    ensures \old(this)->y ≥ x; */
__inline void A::Ctor(struct A *this)
{
  this->y = 2;
  return;
}

/*@ requires \valid(this);
    requires __other ≡ \null ∨ (\valid(__other) ∧ __inv(__other));
    requires \valid(__other);
    ensures \old(this)->y ≥ x;
 */
__inline void A::Ctor(struct A *this, struct A *__other)
{
  this->y = __other->y;
  return;
}

/*@ requires \valid(this);
    requires this->y ≥ x;
    requires __other ≡ \null ∨ (\valid(__other) ∧ __inv(__other));
    requires \valid(__other);
    ensures \old(this)->y ≥ x;
    ensures \valid(\result) ⇒ __inv(\result);
 */
__inline struct A *operator=(struct A *this, struct A *__other)
{
  struct A *__res;
  this->y = __other->y;
  __res = this;
  return __res;
}

/*@ requires \valid(this);
    requires this->y ≥ x; */
__inline void A::Dtor(struct A *this)
{
  return;
}


