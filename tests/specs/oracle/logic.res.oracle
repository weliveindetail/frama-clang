[kernel] Parsing tests/specs/logic.cc (external front-end)
Now output intermediate result
/* Generated by Frama-C */
/*@ logic ℤ f(int x) = x + 1;
 */
/*@ predicate p{L}(char *x) = \valid(x);
 */
/*@ predicate foo(int x) = \true;
 */
/*@
lemma f_def{L}:
  ∀ int *x, int *y, int z, int *t;
    f(*x) ≡ *x + 1 ∧ f(*y) ≡ *y + 1 ∧ f(z) ≡ z + 1 ∧
    f(*t) ≡ *t + 1;
 */
/*@ lemma f_test: 4 ≡ f((int)3);
 */
/*@ lemma f_inf: ∀ int x; 0 < x ≤ f(x);
 */
/*@ logic long long g(long long x) = (long long)((int)(x - 1));
 */
/*@ behavior default:
      ensures \result ≡ f(\old(x)); */
int g(int x)
{
  int __retres;
  __retres = x + 1;
  return __retres;
}

/*@ requires foo: p(x); */
int h(char *x)
{
  int __retres;
  __retres = (int)*x;
  return __retres;
}


