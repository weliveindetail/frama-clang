Now output intermediate result
/* Generated by Frama-C */
struct A {
   int x ;
};
void A::Ctor(struct A *this);

void A::Ctor(struct A *this, struct A const *x);

/*@ requires \valid(this); */
void A::Ctor(struct A *this)
{
  return;
}

/*@ requires \separated(this, x);
    requires \valid(this);
    requires \valid(x); */
void A::Ctor(struct A *this, struct A const *x)
{
  this->x = x->x;
  return;
}

void f(void)
{
  return;
}

int main(void)
{
  int __retres;
  struct A a;
  A::Ctor(& a);
  __retres = 0;
  return __retres;
}


